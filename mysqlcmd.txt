mysql commands

create table shares(
id int(11) primary key not null auto_increment,
user_id int(11) not null,
title varchar(255) not null,
body text,
link varchar(255) not null,
share_date timestamp default current_timestamp not null
);

alter table shares
add foreign key (user_id)
references users(id);

insert into shares(user_id,title,body,link) values(1,'Share One','This is the body of the first share', 'http://example.com');

